pipeline {
  agent any
  stages {
    stage ('build') {
      echo SB is building first project
    }
    stage ('deploy') {
      echo This stage will Deploy the SB project
    }
    stage ('test') {
      echo Testing phase
    }
    stage ('release') {
      echo Release this successful build to the customers
    }
  }
}
